# E2E Navigation Test Cases — Role: Client User

> Auto-generated by e2e-nav-test skill
> Framework: Next.js 14.2 App Router
> Generated: 2026-04-25
> Role: Client User (client portal)

End-to-end acceptance tests for HypeFlow OS — client portal.
Tests are ordered by dependency — if a test fails, all dependent tests are automatically **BLOCKED**.

## Prerequisites

- **Start server**: `cd hypeflow-os && npm run dev` (port 3000)
- **Base URL**: `http://localhost:3000`
- **Auth mode**: Demo mode — `NEXT_PUBLIC_SUPABASE_URL` must contain `placeholder` (no real credentials needed)
- **Demo mode note**: In demo mode, the middleware bypasses all auth. Client pages are accessible by navigating directly — no login step needed.
- **Screenshots on failure**: saved to `docs/screenshots/` subfolder
- **Browser mode**: headless

## Detected Stack

- **Framework**: Next.js 14.2 App Router
- **Auth**: Supabase Auth + `@supabase/ssr` (middleware-based session)
- **UI Library**: Tailwind CSS + custom CSS variables
- **State**: Zustand + tRPC / React Query
- **Roles detected**: Agency User (`users` table) · Client User (`client_users` table)
- **Routes discovered**: 32 total (22 admin, 5 client, 3 public, 2 dynamic)

## Role Matrix

| Feature | Agency User | Client User |
|---|---|---|
| `/admin/*` (22 pages) | yes | no → redirect `/client/dashboard` |
| `/client/*` (5 pages) | no → redirect `/admin/dashboard` | yes |
| `/login` | redirected to `/admin/dashboard` after auth | redirected to `/client/dashboard` after auth |
| `/f/[slug]`, `/portal/[token]` | yes | yes |

---

## Dependency Tree

```
T01 Client dashboard — direct navigation (demo mode)
 ├─ T02 Navigate to ROI & Métricas (/client/roi)
 ├─ T03 Navigate to As Minhas Leads (/client/leads)
 ├─ T04 Navigate to Calls (/client/calls)
 ├─ T05 Navigate to Pipeline (/client/pipeline)
 └─ T06 Logout from client area
T07 Client accessing /admin → redirect ⚠️ SKIP in demo mode — requires real Supabase
```

If a parent test fails, all its children are automatically **BLOCKED** and skipped.

---

## Test Flows

### T01: Client dashboard — direct navigation (demo mode)

**Dependencies**: None
**Blocks**: T02–T06

- [ ] Navigate directly to `http://localhost:3000/client/dashboard`
- [ ] Verify the URL stays at `/client/dashboard` (no redirect in demo mode)
- [ ] Verify the client sidebar is visible (contains "Dashboard", "ROI & Métricas", "As Minhas Leads", "Calls", "Pipeline")
- [ ] Verify the HypeFlow logo and "Portal do Cliente" label are visible in the sidebar
- [ ] Verify the live indicator dot is visible in the sidebar
- [ ] Verify at least one KPI card or chart is rendered on the dashboard

---

### T02: Navigate to ROI & Métricas

**Dependencies**: T01
**Blocks**: None

- [ ] In the client sidebar, click "ROI & Métricas"
- [ ] Verify the URL changes to `/client/roi`
- [ ] Verify the page renders without a full-page error
- [ ] Verify at least one metric, chart, or KPI element is visible

---

### T03: Navigate to As Minhas Leads

**Dependencies**: T01
**Blocks**: None

- [ ] In the client sidebar, click "As Minhas Leads"
- [ ] Verify the URL changes to `/client/leads`
- [ ] Verify the page renders without a full-page error
- [ ] Verify the leads list or empty state is visible

---

### T04: Navigate to Calls

**Dependencies**: T01
**Blocks**: None

- [ ] In the client sidebar, click "Calls"
- [ ] Verify the URL changes to `/client/calls`
- [ ] Verify the page renders without a full-page error

---

### T05: Navigate to Pipeline

**Dependencies**: T01
**Blocks**: None

- [ ] In the client sidebar, click "Pipeline"
- [ ] Verify the URL changes to `/client/pipeline`
- [ ] Verify the page renders without a full-page error

---

### T06: Logout from client area

**Dependencies**: T01
**Blocks**: None

- [ ] Navigate back to `/client/dashboard` if not already there
- [ ] In the client sidebar, find and click the logout button (bottom of sidebar, with LogOut icon)
- [ ] Wait for navigation to complete
- [ ] Verify the URL is `http://localhost:3000/login`

---

### Access Denial Tests

> ⚠️ **These tests require real Supabase credentials. SKIP in demo mode.**
> In demo mode, the middleware bypasses all auth checks (`isPreview` returns early).
> Add these to a separate CI job when the staging Supabase project is configured.

---

### T07: Client user accessing /admin → redirect to /client/dashboard

**Dependencies**: None (authenticated as client user)
**Blocks**: None
**Requires**: Real Supabase — SKIP in demo mode

- [ ] Log in as a real client user (in `client_users` table, not `users` table)
- [ ] Navigate to `http://localhost:3000/admin/dashboard`
- [ ] Verify the middleware redirects to `/client/dashboard`
- [ ] Verify the client dashboard content is shown (client sidebar visible)
- [ ] Verify admin dashboard content is NOT visible (no admin sidebar)

---

## Observations

Executed: 2026-04-25 — Real Supabase credentials (test-client@hypeflow.dev, client_users table).

### Failed Tests

_None._

### Blocked Tests

| Test | Reason |
|------|--------|
| _None_ | — |

### Summary

- Total: 7
- Passed: 7
- Failed: 0
- Blocked: 0
- Skipped (demo mode): 0 (T07 tested with real Supabase)
