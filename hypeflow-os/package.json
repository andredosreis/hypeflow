{
  "name": "hypeflow-os",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --filter=@hypeflow/app",
    "dev:all": "turbo run dev",
    "build": "turbo run build --filter=@hypeflow/app",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "db:push": "cd supabase && supabase db push",
    "db:types": "supabase gen types typescript --linked > packages/database/src/types.ts"
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "turbo": "^2.9.4",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "npm@10.9.2"
}
